<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify OTP | E-Vaulto</title>

  <style>
    :root {
      --bg-dark: #0b0f0c;
      --bg-card: #121816;
      --text-main: #e6e6e6;
      --text-muted: #9aa5a0;
      --accent: #22c55e; /* 🟢 secure green */
      --accent-soft: rgba(34, 197, 94, 0.25);
      --border: #1f2a25;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", system-ui, sans-serif;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #16201b, #050706);
      display: flex;
      justify-content: center;
      align-items: center;
      color: var(--text-main);
      animation: fadeIn 1s ease forwards;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 40px;
      width: 100%;
      max-width: 360px;
      box-shadow: 0 0 35px rgba(0,0,0,0.7);
      animation: slideUp 1s ease forwards;
    }

    @keyframes slideUp {
      from {
        transform: translateY(40px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .logo {
      text-align: center;
      font-size: 2.2rem;
      font-weight: 800;
      margin-bottom: 8px;
    }

    .logo span {
      color: var(--accent);
      text-shadow: 0 0 16px var(--accent-soft);
    }

    .subtitle {
      text-align: center;
      font-size: 0.9rem;
      color: var(--text-muted);
      margin-bottom: 25px;
    }

    form input {
      width: 100%;
      padding: 12px 14px;
      margin-bottom: 16px;
      background: #0b0f0c;
      border: 1px solid var(--border);
      border-radius: 8px;
      color: var(--text-main);
      font-size: 0.95rem;
      letter-spacing: 3px;
      text-align: center;
      transition: all 0.25s ease;
    }

    form input:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 14px var(--accent-soft);
    }

    .btn {
      width: 100%;
      padding: 14px;
      background: var(--accent);
      border: none;
      border-radius: 10px;
      font-weight: 700;
      cursor: pointer;
      color: #04110a;
      transition: all 0.3s ease;
      box-shadow: 0 0 22px var(--accent-soft);
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 0 35px var(--accent);
    }

    .back {
      margin-top: 18px;
      text-align: center;
      font-size: 0.85rem;
    }

    .back a {
      color: var(--accent);
      text-decoration: none;
    }

    .back a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>

  <div class="card">
    <div class="logo">E-<span>Vaulto</span></div>
    <p class="subtitle">Enter the 4-digit code sent to your email</p>

    <form method="POST" action="/verify-otp">
      <input type="hidden" name="email" value="<%= email %>" />

      <input
        type="text"
        name="otp"
        placeholder="••••"
        maxlength="4"
        required
      />

      <button class="btn" type="submit">Verify OTP</button>
    </form>

    <div class="back">
      <a href="/forgot-password">← Resend OTP</a>
    </div>
  </div>

</body>
</html>
